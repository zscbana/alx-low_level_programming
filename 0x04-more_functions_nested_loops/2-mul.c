#include "main.h"

/**
 * mul - function used to multiple two numbers
 * @a: first number
 * @b: secound number
 *
 * Return: return results
 */

int mul(int a, int b)
{
	int a, b, num;
		num = a * b;
	return (num);
}
