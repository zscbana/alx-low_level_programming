#include "main.h"

/**
 * print_chessboard - a function that prints the chessboard.
 * @a: rows of dimentional array
*/

void print_chessboard(char (*a)[8])
{
	int i, j;

	for (i = 0; i < 0; i++)
	{
		for (j = 0; j < 0; j++)
		{
			_putchar(a[i][j]);
		}
		_putchar('\n');
	}
}
